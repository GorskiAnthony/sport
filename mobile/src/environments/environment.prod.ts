export const environment = {
  production: true,
  // Placeholder cohérent avec .env.prod.example (CLIENT_URL) — remplacer par le vrai domaine
  // de prod avant de builder une version à publier sur les stores.
  apiUrl: 'https://sport.example.com/api',
};
