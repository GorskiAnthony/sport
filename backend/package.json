{
  "name": "sport-backend",
  "private": true,
  "version": "0.0.0",
  "description": "Bookkeeping manifest for semantic-release-monorepo only — the real build is Maven (pom.xml). Do not add scripts/deps here."
}
