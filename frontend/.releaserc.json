{
  "branches": ["main", { "name": "dev", "prerelease": true }],
  "tagFormat": "frontend-v${version}",
  "extends": "semantic-release-monorepo",
  "plugins": [
    ["@semantic-release/commit-analyzer", { "preset": "conventionalcommits" }],
    ["@semantic-release/release-notes-generator", { "preset": "conventionalcommits" }],
    ["@semantic-release/changelog", { "changelogFile": "CHANGELOG.md" }],
    ["@semantic-release/npm", { "npmPublish": false }],
    [
      "@semantic-release/exec",
      {
        "prepareCmd": "docker build -t $FRONTEND_IMAGE:${nextRelease.version} .",
        "publishCmd": "docker push $FRONTEND_IMAGE:${nextRelease.version} && if [ \"${nextRelease.channel}\" = \"dev\" ]; then docker tag $FRONTEND_IMAGE:${nextRelease.version} $FRONTEND_IMAGE:dev && docker push $FRONTEND_IMAGE:dev; else docker tag $FRONTEND_IMAGE:${nextRelease.version} $FRONTEND_IMAGE:latest && docker push $FRONTEND_IMAGE:latest; fi"
      }
    ],
    [
      "@semantic-release/git",
      {
        "assets": ["package.json", "CHANGELOG.md"],
        "message": "chore(release): frontend v${nextRelease.version} [skip ci]\n\n${nextRelease.notes}"
      }
    ],
    "@semantic-release/github"
  ]
}
